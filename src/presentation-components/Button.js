import styled from 'styled-components';

export default styled.input`
	padding: 1rem;
	font-weight: bold;
	font-size: 1rem;
	border: 0;
	background: #000;
	color: #fff;
	cursor: pointer;
	transition: all ease-out 0.5s;

	&:hover {
		background: #fff;
		color: #000;
	}
`;